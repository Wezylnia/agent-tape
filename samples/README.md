# Samples

Sample projects should stay small and deterministic.

Planned samples:

- `dotnet-failing-tests`: a tiny test project with one failing test for `agenttape record -- dotnet test`
- `agent-session-demo`: scripted command sequence that demonstrates command capture, git diff, redaction, and report generation

Do not add large binaries, generated reports, or real secrets to this directory.
